# Improved Pipeline Configuration
# Tương tự generate_llm_code_config.cfg nhưng với thêm options cho improvements

# Original settings
log_dir=../samples/example_samples/M/llm_generated_paper
source_file=../samples/example_samples/M/file.c
num_funcs=5
llm=codestral-2508
output_dir=../samples/example_samples/M/llm_generated_paper
trials=1

# New improvement settings
AUTO_FIX=true          # Enable auto-fixing compilation errors
RUN_TESTS=true         # Run compilation tests
PARALLEL=false         # Enable parallel processing (experimental)
USE_CACHE=true         # Use caching for performance
MAX_FIX_ATTEMPTS=3     # Maximum auto-fix attempts per function

